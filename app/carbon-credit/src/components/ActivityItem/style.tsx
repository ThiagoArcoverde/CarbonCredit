import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {},
  content: {},
  line: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "flex-start",
  },
});
